[package]
name = "symbios-wasm-probe"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
symbios-tensor = { path = "../../upstream" }
symbios-ground = "0.4"
getrandom = { version = "0.3", features = ["wasm_js"] }
